apiVersion: v1
kind: Service
metadata:
  name: {{ include "gql-gateway.fullname" . }}
  labels:
    {{- include "gql-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gql-gateway.selectorLabels" . | nindent 4 }}
