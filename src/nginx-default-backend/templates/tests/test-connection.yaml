apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-default-backend.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-default-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-default-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
